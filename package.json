{
  "name": "combo-test-1",
  "version": "0.0.1",
  "dependencies": {
    "express": "4.13.3",
    "nodemon": "^1.11.0",
    "socket.io": "^1.7.4"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  }
}
