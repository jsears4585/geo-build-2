{
  "name": "geo-game-backend",
  "version": "0.0.1",
  "dependencies": {
    "express": "4.13.3",
    "mongodb": "^2.2.31",
    "mongoose": "^4.11.6",
    "nodemon": "^1.11.0",
    "socket.io": "^1.7.4"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  }
}
